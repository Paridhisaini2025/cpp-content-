                                            ASSIGNMENT-1

1. Write a C++ program to demonstrate the use of const_cast by modifying a constant variable.
cpp
Copy
Edit
#include <iostream>
using namespace std;

void modifyValue(const int* ptr) {
    int* modPtr = const_cast<int*>(ptr);
    *modPtr = 100;
}

int main() {
    const int x = 10;
    cout << "Before: " << x << endl;
    modifyValue(&x);
    cout << "After: " << x << endl; // Undefined behavior
    return 0;
}
2. Write a C++ program to demonstrate the use of reinterpret_cast to convert an int to a char.
cpp
Copy
Edit
#include <iostream>
using namespace std;

int main() {
    int a = 65;
    char* ch = reinterpret_cast<char*>(&a);
    cout << "Character: " << *ch << endl;
    return 0;
}
3. Write a C++ program to demonstrate the use of C-style casting by converting an int to a double.
cpp
Copy
Edit
#include <iostream>
using namespace std;

int main() {
    int num = 10;
    double d = (double)num;
    cout << "Double: " << d << endl;
    return 0;
}
4. Write a C++ program to print the numbers from 1 to 10 using a for loop.
cpp
Copy
Edit
#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 10; i++)
        cout << i << " ";
    cout << endl;
    return 0;
}
5. Write a C++ program to calculate the sum of the first 50 natural numbers using a while loop.
cpp
Copy
Edit
#include <iostream>
using namespace std;

int main() {
    int sum = 0, i = 1;
    while (i <= 50) {
        sum += i;
        i++;
    }
    cout << "Sum: " << sum << endl;
    return 0;
}
6. Write a C++ program to print the multiplication table of a given number using a do-while loop.
cpp
Copy
Edit
#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter number: ";
    cin >> n;
    int i = 1;
    do {
        cout << n << " * " << i << " = " << n * i << endl;
        i++;
    } while (i <= 10);
    return 0;
}
